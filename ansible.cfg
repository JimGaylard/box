# Every playbook targets the implicit localhost; there is no inventory to
# parse, so silence the two warnings ansible-playbook prints about that.
[defaults]
localhost_warning = False

[inventory]
inventory_unparsed_warning = False
